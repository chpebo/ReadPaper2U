# Dependencies for the helper scripts only.
# The web app itself (index.html) has no Python dependencies.
#
# find-section.py uses the Python standard library only and needs nothing
# from this file. The packages below are required only by process_avatar.py.

pillow>=10.0.0
pillow-heif>=0.16.0
rembg>=2.0.50
